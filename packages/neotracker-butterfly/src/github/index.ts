import { api } from './api';
import { authenticate } from './authenticate';
import { utils } from './utils';

// tslint:disable-next-line export-name
export const github = {
  api,
  authenticate,
  utils,
};
