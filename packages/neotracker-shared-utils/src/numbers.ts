import BigNumber from 'bignumber.js';

const ZERO = new BigNumber('0');
const D = new BigNumber('100000000');
const TOTAL_NEO = new BigNumber('100000000');
const INVALID_INDEX = new BigNumber(-1);

export const numbers = {
  ZERO,
  D,
  TOTAL_NEO,
  INVALID_INDEX,
};
